{
  "name": "doomingo",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev:server": "bun --watch index.ts",
    "start": "bun index.ts",
    "migrate": "prisma migrate dev",
    "generate": "prisma generate"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "hono": "^4.6.12"
  }
}