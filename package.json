{
  "name": "doomingo",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev:server": "bun --watch index.ts",
    "start": "bun index.ts",
    "migrate": "prisma migrate dev",
    "generate": "prisma generate",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "hono": "^4.6.12",
    "zod": "^3.23.8"
  }
}