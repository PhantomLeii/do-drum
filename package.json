{
  "name": "doomingo",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev:server": "bun --watch index.ts",
    "start": "bun index.ts",
    "migrate": "prisma migrate dev",
    "generate": "prisma generate"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "hono": "^4.6.12"
  }
}